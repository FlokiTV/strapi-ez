{
  "name": "strapi-ez",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --platform=browser --outfile=dist/index.js",
    "build:esm": "esbuild index.js --platform=browser --format=esm --outfile=dist/index.mjs"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "qs": "^6.11.2"
  },
  "devDependencies": {
    "esbuild": "^0.18.11"
  }
}
